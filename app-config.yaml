app:
  title: Morgan Stanley KaaS Platform
  baseUrl: http://localhost:3000
  support:
    url: https://github.com/morganstanley/kaas-platform/issues
    items:
      - title: KaaS Platform Support
        icon: chat
        links:
          - url: https://teams.microsoft.com/l/channel/kaas-support
            title: '#kaas-support on Teams'
      - title: Platform Engineering
        icon: group
        links:
          - url: https://backstage.ms.com/catalog/default/group/platform-engineering
            title: Platform Engineering Team

organization:
  name: Morgan Stanley

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    img-src: ["'self'", 'data:', 'https://avatars.githubusercontent.com']
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: better-sqlite3
    connection: ':memory:'
  # workingDirectory: /tmp

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}

proxy:
  endpoints:
    # ArgoCD API proxy
    '/argocd/api':
      target: ${ARGOCD_BASE_URL:-http://localhost:8080}/api/v1/
      changeOrigin: true
      headers:
        Cookie: argocd.token=${ARGOCD_AUTH_TOKEN:-placeholder}
    # Prometheus API proxy
    '/prometheus/api':
      target: ${PROMETHEUS_URL:-http://localhost:9090}
      changeOrigin: true
    # Grafana API proxy
    '/grafana/api':
      target: ${GRAFANA_URL:-http://localhost:3001}
      changeOrigin: true
      headers:
        Authorization: Bearer ${GRAFANA_TOKEN:-placeholder}
    # Vault API proxy
    '/vault/api':
      target: ${VAULT_ADDR:-http://localhost:8200}
      changeOrigin: true
      headers:
        X-Vault-Token: ${VAULT_TOKEN:-placeholder}

techdocs:
  builder: 'local'
  generator:
    runIn: 'docker'
  publisher:
    type: 'local'

# ============================================================================
# Authentication - Microsoft Entra ID (Azure AD)
# ============================================================================
auth:
  environment: development
  providers:
    guest: {}
    microsoft:
      development:
        clientId: ${AZURE_CLIENT_ID}
        clientSecret: ${AZURE_CLIENT_SECRET}
        tenantId: ${AZURE_TENANT_ID}
        domainHint: morganstanley.com
        additionalScopes:
          - 'User.Read'
          - 'GroupMember.Read.All'

# ============================================================================
# Kubernetes Plugin Configuration
# ============================================================================
kubernetes:
  serviceLocatorMethod:
    type: 'multiTenant'
  clusterLocatorMethods:
    - type: 'config'
      clusters:
        # Azure Management Cluster
        - url: ${AKS_MGMT_CLUSTER_URL}
          name: ms-azure-mgmt
          authProvider: 'azure'
          title: 'Azure Management Cluster'
          dashboardUrl: https://portal.azure.com
          dashboardApp: standard
          caData: ${AKS_MGMT_CA_DATA}
        # AWS Management Cluster
        - url: ${EKS_MGMT_CLUSTER_URL}
          name: ms-aws-mgmt
          authProvider: 'aws'
          title: 'AWS Management Cluster'
          dashboardUrl: https://console.aws.amazon.com/eks
          dashboardApp: standard
          caData: ${EKS_MGMT_CA_DATA}
        # GCP Management Cluster
        - url: ${GKE_MGMT_CLUSTER_URL}
          name: ms-gcp-mgmt
          authProvider: 'google'
          title: 'GCP Management Cluster'
          dashboardUrl: https://console.cloud.google.com/kubernetes
          dashboardApp: standard
          caData: ${GKE_MGMT_CA_DATA}

scaffolder:
  defaultAuthor:
    name: Morgan Stanley KaaS Platform
    email: kaas-platform@morganstanley.com
  defaultCommitMessage: 'chore: scaffolder action from KaaS Platform'

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow:
        - Component
        - System
        - API
        - Resource
        - Location
        - Template
        - Group
        - User
  locations:
    # ========================================================================
    # KaaS Platform Templates
    # ========================================================================
    # Cluster Provisioning
    - type: file
      target: ../../templates/kubernetes-cluster/template.yaml
      rules:
        - allow: [Template]

    # Day 2 Operations
    - type: file
      target: ../../templates/cluster-upgrade/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/cluster-scale/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/cluster-destroy/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/namespace-request/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/addon-management/template.yaml
      rules:
        - allow: [Template]

    # ========================================================================
    # Platform Catalog Entities
    # ========================================================================
    - type: file
      target: ../../catalog/systems.yaml
      rules:
        - allow: [System, Resource, Component]
    - type: file
      target: ../../catalog/groups.yaml
      rules:
        - allow: [Group, User]

    # ========================================================================
    # Example data (can be removed in production)
    # ========================================================================
    - type: file
      target: ../../examples/entities.yaml
    - type: file
      target: ../../examples/template/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

    ## Uncomment these lines to add an example org
    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/acme-corp.yaml
    #   rules:
    #     - allow: [User, Group]

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: true
